'use client'

import { FormEvent, useEffect, useState } from 'react'
import { PortalHeader } from '@/components/PortalChrome'

type Complaint={complaint_number:string;registration_number:string;serial_number:string;model_code:string;product_name:string;complaint_type:string;problem_description:string;status:string;preferred_visit_date:string|null;preferred_contact_time:string|null;service_city:string|null;service_state:string|null;created_at:string}
async function getComplaint(identifier:string):Promise<Complaint|null>{const url=process.env.NEXT_PUBLIC_SUPABASE_URL;const key=process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY;if(!url||!key)throw new Error('Service portal is not configured.');const response=await fetch(`${url}/rest/v1/rpc/get_complaint_tracking`,{method:'POST',headers:{apikey:key,Authorization:`Bearer ${key}`,'Content-Type':'application/json'},body:JSON.stringify({p_complaint_number:identifier}),cache:'no-store'});const body=await response.text();if(!response.ok)throw new Error(body||`Complaint lookup failed (${response.status}).`);const data=body?JSON.parse(body):[];return data?.[0]??null}
const fmt=(value:string|null|undefined)=>value?new Date(value.includes('T')?value:`${value}T00:00:00`).toLocaleDateString('en-IN',{day:'2-digit',month:'short',year:'numeric'}):'—';const statusLabel:Record<string,string>={received:'Received',assigned:'Assigned',technician_visit:'Technician Visit',under_service:'Under Service',resolved:'Resolved',closed:'Closed'};const statusSteps=['received','assigned','technician_visit','under_service','resolved','closed']
function ComplaintTimeline({status}:{status:string}){const current=statusSteps.indexOf(status);const activeIndex=current<0?0:current;return <div className="customerTimeline cleanTimeline">{statusSteps.map((step,index)=><div className={`customerTimelineStep ${index<=activeIndex?'active':''}`} key={step}><div className="customerTimelineDot">{index<=activeIndex?'✓':index+1}</div><span>{statusLabel[step]}</span></div>)}</div>}
export default function ComplaintTrackingPage(){const [number,setNumber]=useState('');const [complaint,setComplaint]=useState<Complaint|null>(null);const [loading,setLoading]=useState(false);const [error,setError]=useState('');useEffect(()=>{const preset=new URLSearchParams(window.location.search).get('complaint')||'';if(preset){setNumber(preset);void lookup(preset)}},[]);async function lookup(value:string){const cleaned=value.trim().toUpperCase();if(!cleaned){setError('Enter a complaint number, registration number or serial number.');setComplaint(null);return}setLoading(true);setError('');setComplaint(null);try{const result=await getComplaint(cleaned);if(!result)throw new Error('No complaint found for this number or registered product.');setComplaint(result)}catch(e){setError(e instanceof Error?e.message:'Unable to retrieve complaint details.')}finally{setLoading(false)}}function submit(event:FormEvent){event.preventDefault();void lookup(number)}return <div className="app customerPage portalTrackPage cleanPortalPage"><PortalHeader/><main className="customerMain cleanMain"><section className="customerHero compactHero cleanHeroBanner"><span className="customerEyebrow">COMPLAINT STATUS</span><h1>Track your complaint.</h1><p>Use your complaint number, registration number or registered serial number.</p></section><section className="customerSection customerFormPanel cleanPageCard"><span className="customerBadge">Complaint Status</span><h2>Find your service request</h2><form onSubmit={submit}><label>Complaint number, registration number or serial number</label><input className="customerInput" required autoCapitalize="characters" autoComplete="off" value={number} onChange={e=>setNumber(e.target.value.toUpperCase())} placeholder="Enter any registered number"/><button className="customerButton customerButtonPrimary" disabled={loading} type="submit">{loading?'Checking…':'Check Status'} <span>→</span></button></form>{error&&<div className="customerError">{error}</div>}</section>{complaint&&<div className="customerResults cleanResults"><section className="customerSection customerStatusCard cleanPageCard"><div className="customerCheck">✓</div><span className="customerBadge customerBadgeSuccess">Complaint found</span><h2>{statusLabel[complaint.status]||complaint.status}</h2><p>Your service request has been successfully retrieved.</p><div className="customerNumberBox cleanNumberBox"><span>Complaint Number</span><strong>{complaint.complaint_number}</strong></div><ComplaintTimeline status={complaint.status}/></section><section className="customerSection cleanPageCard"><span className="customerBadge">Product</span><h2>{complaint.model_code||'OLITEC Product'}</h2><div className="customerInfoList cleanInfoList"><div><span>Product</span><strong>{complaint.product_name||'—'}</strong></div><div><span>Serial number</span><strong>{complaint.serial_number}</strong></div><div><span>Registration number</span><strong>{complaint.registration_number||'—'}</strong></div></div></section><section className="customerSection cleanPageCard"><span className="customerBadge">Complaint details</span><h2>{complaint.complaint_type}</h2><div className="customerInfoList cleanInfoList"><div><span>Registered</span><strong>{fmt(complaint.created_at)}</strong></div><div><span>Preferred visit</span><strong>{fmt(complaint.preferred_visit_date)}</strong></div><div><span>Contact time</span><strong>{complaint.preferred_contact_time||'—'}</strong></div></div><div className="customerDescription"><span>Problem description</span><p>{complaint.problem_description}</p></div></section></div>}</main></div>}
