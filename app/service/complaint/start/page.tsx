'use client'

import { FormEvent, useState } from 'react'
import { PortalIcon, PortalHeader } from '@/components/PortalChrome'

export default function ComplaintStartPage(){const [identifier,setIdentifier]=useState('');function submit(event:FormEvent){event.preventDefault();const value=identifier.trim().toUpperCase();if(value)window.location.href=`/service/complaint?identifier=${encodeURIComponent(value)}`};return <div className="app customerPage portalComplaintPage cleanPortalPage"><PortalHeader/><main className="customerMain cleanMain"><div className="customerSplitLayout cleanSplitLayout"><section className="customerHero customerHeroVisual cleanHero"><div className="customerHeroCopy"><div className="customerHeroIcon"><PortalIcon type="wrench"/></div><span className="customerEyebrow">SERVICE SUPPORT</span><h1>Report a<br/>Problem</h1><p>Use your OLITEC registration number or product serial number to register a service complaint.</p><div className="customerHeroPoints"><span>✓ Quick complaint registration</span><span>✓ Warranty-linked service</span><span>✓ Track your request</span></div></div><div className="customerHeroImage"><img src="/olitec-service-visual.svg" alt="OLITEC service support"/></div></section><section className="customerSection customerFormPanel cleanFormPanel"><span className="customerBadge">Service Complaint</span><h2>Find your product</h2><p>Enter either the registration number or the serial number of your registered OLITEC product.</p><form onSubmit={submit}><label>Registration number or serial number</label><input className="customerInput" required autoCapitalize="characters" autoComplete="off" value={identifier} onChange={e=>setIdentifier(e.target.value.toUpperCase())} placeholder="Enter registration or serial number"/><button className="customerButton customerButtonPrimary" type="submit">Continue <span>→</span></button></form></section></div></main></div>}
